import os
import time
from dotenv import load_dotenv
from telegram import Bot
from telegram.error import TelegramError

# Load environment variables
load_dotenv()

# Get bot token and chat id from .env file
BOT_TOKEN = os.getenv("8372538783:AAEWx9WApRSbvmnEfEByeEftS1iVBYvm6fg")
CHAT_ID = os.getenv("8143587403")

# Create bot instance
bot = Bot(token=BOT_TOKEN)

def send_start_message():
    message = (
        "💥 *POWERED_BY PASIYA-MD FOREX BOT WRACKING* 💥\n\n"
        "✅ Bot is now running successfully!\n"
        "📊 Ready to send Forex signals and updates!"
    )
    try:
        bot.send_message(chat_id=CHAT_ID, text=message, parse_mode="Markdown")
        print("✅ Message sent successfully to Telegram!")
    except TelegramError as e:
        print(f"❌ Error sending message: {e}")

if __name__ == "__main__":
    print("🚀 Starting PASIYA-MD FOREX BOT WRACKING...")
    send_start_message()
    while True:
        # You can later add trading or signal logic here
        time.sleep(10)
